export enum PAGE_PATH {
  ABOUT = "/about",
  SINGLE = "/single/:id",
  ADD_BLOG = "/add-blog",
  CATEGORIES = "/category/:topic",
  USERPAGE = "/user-page/:id",
  BLOGERPAGE = "/blogger/:id",
  BLOGSFLOW = "/:category",
}
